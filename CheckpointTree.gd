extends Area

func _on_Area_body_entered(body):
	pass
	#Checkpoint.last_position = transform.origin
	#var material = $MeshInstance.get_surface_material(0)
	#material.albedo_color = Color("green")
	#$MeshInstance.set_surface_material(0, material)
